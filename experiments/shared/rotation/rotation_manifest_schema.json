{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "ARBITRARY_ORTHOGONAL_ROTATION_MANIFEST_V1",
  "type": "object",
  "required": ["schema_version", "torch_version", "dimension", "generator", "status", "matrices"],
  "properties": {
    "schema_version": {"const": "ARBITRARY_ORTHOGONAL_ROTATION_MANIFEST_V1"},
    "dimension": {"const": 128},
    "status": {"enum": ["PASS", "FAIL"]},
    "matrices": {
      "type": "array",
      "minItems": 3,
      "items": {
        "type": "object",
        "required": ["name", "seed", "determinant", "max_abs_rt_r_minus_i", "frobenius_rt_r_minus_i", "sha256_fp64_c_order_bytes", "orthogonality_gate"]
      }
    }
  }
}
